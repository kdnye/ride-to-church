{
  "name": "ride-to-church",
  "version": "0.1.0",
  "description": "Ride-to-Church dispatch MVP",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "start:prod": "NODE_ENV=production node server.js",
    "deploy:managed": "echo 'Deploy with your managed host (e.g., Fly.io/Render/Railway) and enable TLS termination at the edge.'",
    "deploy:tls": "echo 'TLS is terminated at managed hosting edge; enforce HTTPS via server redirect + HSTS.'",
    "test": "node --test"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.49.4"
  }
}
